# Anchor — Face Recognition Module
# ==================================
#
# Uses MediaPipe FaceLandmarker for on-device face detection and
# geometric landmark-based recognition.  No dlib or C++ compiler
# required — installs cleanly via pip on all platforms.

opencv-python>=4.8.0
mediapipe>=1.0.0
numpy>=1.24.0
